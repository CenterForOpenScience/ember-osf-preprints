
import { providerAsset } from 'preprint-service/helpers/provider-asset';
import { module, test } from 'qunit';

module('Unit | Helper | provider asset');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = providerAsset([42]);
  assert.ok(result);
});

