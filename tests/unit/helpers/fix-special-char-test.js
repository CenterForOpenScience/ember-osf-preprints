
import { fixSpecialChar } from 'preprint-service/helpers/fix-special-char';
import { module, test } from 'qunit';

module('Unit | Helper | fix special char');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = fixSpecialChar([42]);
  assert.ok(result);
});

