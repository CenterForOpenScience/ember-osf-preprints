import { getKeenFileCounts } from 'preprint-service/utils/analytics';
import { module, test } from 'qunit';

module('Unit | Utility | analytics');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = getKeenFileCounts();
  assert.ok(result);
});
