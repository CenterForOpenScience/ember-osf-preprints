{{title (if theme.isProvider (if theme.preprintWordInTitle (t 'global.provider_brand' name=theme.provider.name) theme.provider.name) (t 'global.brand')) separator=(t 'application.separator')}}
{{head-tags headTags=headTags}}

{{#if theme.isProvider}}
    <style>
        {{#if theme.isDomain}}
            @import url('/assets/provider_styles.css');
        {{else}}
            @import url('/preprints/assets/provider_styles.css');
        {{/if}}
    </style>
    <style>
        @import url('{{provider-asset theme.id 'style.css'}}');
    </style>
    {{preprint-navbar-branded model=theme.provider}}
{{else}}
    {{new-osf-navbar signupUrl=theme.signupUrl loginAction=(action 'login')}}
{{/if}}

{{maintenance-banner}}

{{outlet}}

{{#if theme.isProvider}}
    {{preprint-footer-branded model=theme.provider}}
{{else}}
    {{osf-footer}}
    {{osf-copyright class='text-center'}}
    {{osf-mode-footer}}
{{/if}}
