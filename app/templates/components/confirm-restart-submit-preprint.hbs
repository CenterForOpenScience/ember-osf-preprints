{{#bs-modal open=isOpen body=false header=false footer=false}}
    {{#bs-modal-header}}
        <h3 class="modal-title">Restart Preprint</h3>
    {{/bs-modal-header}}
    {{#bs-modal-body}}
        Are you sure you want to restart this preprint? Your uploaded file and supplemental information will be deleted.
    {{/bs-modal-body}}

    {{#bs-modal-footer}}
        <div align="right">
            <button {{action 'close'}} class="btn btn-default"> Cancel </button>
            <a class="btn btn-danger" {{action 'resetFileUpload'}}>Restart </a>
        </div>
    {{/bs-modal-footer}}
{{/bs-modal}}
