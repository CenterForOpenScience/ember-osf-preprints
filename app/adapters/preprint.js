import ApplicationAdapter from './application';

export default ApplicationAdapter.extend({
//    queryUrlTemplate: '{+host}/{+namespace}/collections{/namespaceId}.{collectionId{/search}{?params}',
//    queryRecordUrlTemplate: '{+host}/{+namespace}/collections{/namespaceId}.{collectionId{/search}',
//
//    urlSegments: {
//        params: function(type, id, snapshot, query) {
//            return query.filter ? 'filter[subject]='+query.filter.subject : '';
//        }
//    }
});
