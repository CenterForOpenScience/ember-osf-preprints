import ContributorModel from 'ember-osf/models/contributor';

/**
 * @module ember-preprint
 * @submodule models
 */

/**
 * Preprint authors. This is basically a contributor with a different name.
 * @class Author
 */
export default ContributorModel.extend({
});
